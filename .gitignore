# Context directory (local documentation, not to be pushed)
Context/

# Local test files
latest.json

# Environment variables (contains sensitive keys)
**/.env
.env

# Signing keys backup (contains password and private key)
**/keys.txt
keys.txt
